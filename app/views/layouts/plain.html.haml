<!DOCTYPE html>
%html{lang: "en"}
  %head
    = render "/shared/head"
  %body{ class: SettingsHelper.feature_on?("style_display.use_manage") && manage_mode? ? "manage-mode" : "" }
    #content{role: "main"}
      .container
        .row
          .col-md-12
            = render "shared/flashes"
            = yield
