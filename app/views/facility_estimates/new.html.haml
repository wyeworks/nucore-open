= content_for :h1 do
  = @estimate.facility

%h2= t(".create")

= render "form", 
  form_url: facility_estimates_path(@estimate.facility), 
  form_method: :post,
  user_collection: [],
  submit_text: t(".add"),
  cancel_url: facility_estimates_path(@estimate.facility)
